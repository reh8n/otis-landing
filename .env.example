# The waitlist uses the Cloudflare D1 binding declared as `DB` in
# .openai/hosting.json. No application secrets are required for local use.
#
# Hosted D1 resources are provisioned and bound by the deployment platform.
